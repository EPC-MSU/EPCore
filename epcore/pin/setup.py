from setuptools import setup, find_packages

setup(
    name='pin',
    packages=["pin"],
    author="mihalin",
    author_email="mihalin@physlab.ru",
    install_requires=[
        'numpy',
    ],
)
